<%- [:notice, :error, :alert].each do |kind| -%>
<%- next if flash[kind].blank? -%>
  <div class="alert alert-dismissable alert-<%= kind == 'notice' ? 'success' : 'danger' %>" >
    <% [flash[kind]].flatten.each do |line| %>
    <div><%= line %></div>
    <% end %>
  </div>
<%- end -%>
